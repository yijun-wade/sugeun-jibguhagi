// ── 가격 기준 (만원, 총 거래금액 기준) ────────────────────
// ⚠️ 절대값 기준: 지역/권역 보정 없음
// 강남 8억과 노원 8억은 맥락이 다르나 동일하게 처리됨
// 향후 개선: 구별 중위가격 기준으로 상대 판단 추가 가능
export const PRICE_HIGH = 80000   // 8억 이상 → 가격대 '높은 편'
export const PRICE_LOW  = 40000   // 4억 이하 → 가격대 '낮은 편'

// ── 면적 기준 ───────────────────────────────────────────
export const MIN_AREA_SQM  = 40      // 전용면적 최소값 (㎡), 소형 제외
export const SQM_TO_PYEONG = 3.3058  // ㎡ → 평 환산 계수 (1평 = 3.3058㎡)

// ── 네트워크 ─────────────────────────────────────────────
export const FETCH_TIMEOUT = 10000   // API 타임아웃 (ms)

// ── 한국 좌표 범위 (지도 유효성 검사) ────────────────────
export const KR_LAT = { min: 33, max: 43 }
export const KR_LON = { min: 124, max: 132 }
